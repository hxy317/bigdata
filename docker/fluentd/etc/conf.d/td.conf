<match cv.test.**>
  @type file
  @id test
  path test.log.*.log
  compress gzip
  symlink_path test.link
  <buffer>
    chunk_limit_size 1m
    flush_at_shutdown true
  </buffer>
</match>

#<match cv.{product,mng_hz}.report.video>
#  @type file
#  @id report_video
#  path report_video.log.*.log
#  compress gzip
#  symlink_path report_video.link
#  <buffer>
#  	chunk_limit_size 512m
#  	flush_at_shutdown true
#  </buffer>
#</match>

